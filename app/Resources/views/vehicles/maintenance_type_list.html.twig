{% extends('base/base_list.html.twig') %}

{% block formVisualization %}

    <div class="row">
        <div class="col-lg-12">
            <div class="m-portlet">
                <div class="m-portlet__head">
                    <div class="m-portlet__head-caption">
                        <div class="m-portlet__head-title">
						<span class="m-portlet__head-icon m--hide">
						<i class="la la-gear"></i>
						</span>
                            <h3 class="m-portlet__head-text">
                                Creazione {{ title }}
                            </h3>
                        </div>
                    </div>
                </div>
                {% include('vehicles/forms/maintenance_type_form.html.twig') %}
            </div>
        </div>
    </div>

{% endblock %}

{% block extended_page_scripts %}
    <script src="{{ asset('assets/customjs/vehicles/maintenance/maintenance_type_list.js') }}"></script>
    <script>
        $(document).ready(function() {
            initializeWidgets();
            GenericFormSubmission($('#form_maintenance_type')).init();
            MaintenanceList.init();
        });
    </script>
{% endblock %}